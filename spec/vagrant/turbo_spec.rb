require 'spec_helper'

describe Vagrant::Turbo do
  it 'has a version number' do
    expect(Vagrant::Turbo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
